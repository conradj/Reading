extends ./layout.pug

block title
  title= title

block content
    heading
        h2= 'Week starting: ' + moment.unix(week.start_date).format('dddd, MMMM Do YYYY')
    include ./includes/articles.pug